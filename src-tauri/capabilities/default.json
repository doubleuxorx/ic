{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum authority for the main window: core events and a native file picker. No filesystem, shell, http or process plugin is enabled; privileged work goes through this application's own validated commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "core:window:allow-destroy"
  ]
}
